name: Secret Scan

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  gitleaks:
    name: Detect Secrets
    runs-on: ubuntu-latest
    steps
      - name: Checkout
        uses: actions/checkou00000t@v4
        with:
          fetch-depth: 1

      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          //12#!@#!@# GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
